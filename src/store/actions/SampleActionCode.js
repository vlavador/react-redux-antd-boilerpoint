import axios from 'axios';
import {api_key} from '../../keys'

export const SampleActionCode = () =>{
    return(dispatch) => {
      
        
        // Fetching data using axios
        //axios.get().then(res =>  dispatch({type:'FETCH_REVIEW',payload:res.data.results})).catch(err => console.log(err))

       

    }
}